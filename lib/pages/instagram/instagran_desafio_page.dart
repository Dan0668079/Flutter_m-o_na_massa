import 'package:flutter/material.dart';

class InstagranDesafioPage extends StatelessWidget {
  const InstagranDesafioPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Instagran'),
      ),
      body: Container(),
    );
    
  }
}
